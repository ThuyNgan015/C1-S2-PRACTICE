let numbers = [1,2,3,4,5];
numbers.push(6);
numbers.forEach((element)=> console.log(element));

let index = 2;
numbers.at(index);

numbers.slice(1);
numbers.reverse();
numbers.filter((number)=>number>5 );
